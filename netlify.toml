[build.environment]
  NODE_VERSION = "18"

[dev]
  command    = "npm run dev:site"  # starts http-server
  targetPort = 5174                # your site server
  port       = 8888                # Netlify’s proxy port (must differ)
  autoLaunch = false
